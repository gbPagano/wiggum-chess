[package]
name = "chess-engine"
version = "0.3.0"
edition = "2024"

[dependencies]
chesslib = { path = "../chesslib" }
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
