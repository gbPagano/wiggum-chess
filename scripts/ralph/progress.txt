## Codebase Patterns
- New workspace crates: use `cargo new --lib <name>` from workspace root — it auto-adds to Cargo.toml members.
- Build check: `cargo build --workspace` covers all crates including newly added ones.
- chesslib-simple board model: `squares[rank][file]`, rank 0 = white's back rank. `Square { rank, file }` both 0-based. All types in `src/lib.rs`.
- `Board::apply_move` returns a new Board (immutable pattern). Captures handled implicitly: `replace()` on destination square overwrites any piece there.
- Pawn direction: White advances +1 rank, Black advances -1 rank. White start rank = 1, Black start rank = 6. Promotion rank for white = 7, black = 0.
- Skip promotion in pawn generation until US-010 — skip moves landing on promote_rank in `pseudo_legal_pawn_moves`.
- En passant file set in `apply_move` for double pushes (white: rank 1→3, black: rank 6→4).

# Ralph Progress Log
Started: Thu Apr  9 13:19:42 PDT 2026
---

## 2026-04-09 - US-001
- Created `chesslib-simple` library crate using `cargo new --lib chesslib-simple`
- `cargo new` automatically added the crate to workspace `members` in root `Cargo.toml`
- Files changed: `Cargo.toml` (workspace member added), `chesslib-simple/` (new crate with `Cargo.toml` and `src/lib.rs`)
- **Learnings for future iterations:**
  - `cargo new --lib` from the workspace root auto-adds the crate to workspace members
  - The new crate is named `chesslib-simple` to distinguish it from `chesslib`
  - `cargo build --workspace` confirms all crates compile together
---

## 2026-04-09 - US-002
- Implemented `Color`, `PieceKind`, `Piece`, `CastlingRights`, `Square`, and `Board` types in `chesslib-simple/src/lib.rs`
- Board uses `[[Option<Piece>; 8]; 8]` matrix — no bitboards
- `en_passant_file: Option<u8>` tracks file for potential en passant capture
- Files changed: `chesslib-simple/src/lib.rs`
- **Learnings for future iterations:**
  - `squares[rank][file]`, rank 0 = white's back rank (rank 1), rank 7 = black's back rank (rank 8)
  - All core types live in `src/lib.rs` — future stories should add modules or extend this file
  - `Board::starting_position()` sets up the full start state with `CastlingRights::all()`
---

## 2026-04-09 - US-003
- Added `Move { from: Square, to: Square }` struct and `Board::apply_move(&self, mv: Move) -> Board`
- `apply_move` moves piece, handles captures implicitly (destination overwritten), toggles side_to_move, updates halfmove clock and fullmove number
- Files changed: `chesslib-simple/src/lib.rs`
- **Learnings for future iterations:**
  - `apply_move` uses immutable pattern (returns new Board via clone), consistent with chesslib design
  - Halfmove clock: reset on pawn move OR capture; increment otherwise
  - Fullmove number: increment only after black's move
  - `en_passant_file` is cleared in `apply_move`; pawn generation (US-004) will set it for double pushes
---

## 2026-04-09 - US-004
- Added `Board::pseudo_legal_pawn_moves()` generating single pushes, double pushes from start rank, and diagonal captures
- Updated `Board::apply_move` to set `en_passant_file` on pawn double pushes (white: rank 1→3, black: rank 6→4)
- Promotion moves skipped (to_rank == promote_rank): handled in US-010
- Files changed: `chesslib-simple/src/lib.rs`
- **Learnings for future iterations:**
  - Pawn direction is +1 rank for white, -1 rank for black; start ranks are 1 (white) and 6 (black)
  - Promotion rank check must skip both quiet and capture moves landing on rank 7 (white) / rank 0 (black)
  - En passant file is stored per-move in `apply_move`; the rank can be inferred from the color whose turn it is to capture
  - 20 tests total now passing in `chesslib-simple`
---
