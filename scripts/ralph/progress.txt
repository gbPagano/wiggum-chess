# Ralph Progress Log
Started: Thu Apr  9 01:47:41 PDT 2026
---

## Codebase Patterns
- `chess-runner/src/main.rs` contains all CLI arg structs (`MatchArgs`, `SprtArgs`, `PgnMatchArgs`) and subcommand logic in one file (~1655 lines)
- Default values for CLI args are set via `#[arg(long, default_value = "...")]` clap annotations on struct fields
- `MatchArgs` and `PgnMatchArgs` are separate structs with similar but distinct fields — changes to one don't affect the other
- `write_csv` and `write_sprt_csv` functions are in `main.rs` and write CSV rows with headers on first write

## 2026-04-09 - US-001
- Changed `MatchArgs.inc` default from `"0"` to `"1000"` in `chess-runner/src/main.rs`
- `SprtArgs` already had the correct defaults (time=10000, inc=100)
- Files changed: `chess-runner/src/main.rs`
- **Learnings for future iterations:**
  - `PgnMatchArgs` also has `time` and `inc` fields with separate defaults — it was not changed for this story (acceptance criteria scoped to `match` and `sprt`)
  - The `write_csv` function in `main.rs` will need a new `time_control` parameter for US-002
  - The `write_sprt_csv` function in `main.rs` will need a `time_control` parameter for US-003
---

## 2026-04-09 - US-002
- Added `time_control: &str` parameter to `write_csv` in `chess-runner/src/main.rs`
- Added `time_control` to both the CSV header row and each data row
- Updated both call sites (positions mode ~line 989, standard mode ~line 1051) to derive label via `match (args.time, args.inc)`: `(60000, 1000) => "LTC"`, `(10000, 100) => "STC"`, `_ => "custom"`
- Files changed: `chess-runner/src/main.rs`
- **Learnings for future iterations:**
  - `run_report` reads CSV columns by positional index (0-7); `start_fen` (index 8) and `time_control` (index 9) are not currently read — US-007 will add that
  - The `MatchRow` struct does not yet have `time_control` field; US-007 will need to add it
  - LTC/STC label derivation uses `match (args.time, args.inc)` — keep consistent in future stories
---

## 2026-04-09 - US-003
- Added `time_control: &str` parameter to `write_sprt_csv` in `chess-runner/src/main.rs`
- Added `time_control` to both the SPRT CSV header row and each data row
- Updated call site (~line 1228) to derive label via `match (args.time, args.inc)`: `(10000, 100) => "STC"`, `(60000, 1000) => "LTC"`, `_ => "custom"`
- Files changed: `chess-runner/src/main.rs`
- **Learnings for future iterations:**
  - SPRT CSV now has `time_control` at index 10 (after `sprt_result` at index 9)
  - US-008 will need to read SPRT CSV `time_control` column in `version-report`
  - LTC/STC label derivation pattern is consistent across `write_csv` and `write_sprt_csv`
---

## 2026-04-09 - US-004
- Changed `benchmark-version.sh` primary match block to use LTC (60000ms + 1000ms)
- Renamed `TIME_MS`/`INC_MS` to `LTC_TIME_MS`/`LTC_INC_MS` and added `STC_TIME_MS`/`STC_INC_MS`
- Updated `run_match` and `run_balanced_match` to accept `time_ms`/`inc_ms` parameters
- Updated SPRT block to use `STC_TIME_MS`/`STC_INC_MS` (prep for US-006)
- Added "Primary benchmark block: LTC" print before the 4 match runs
- Files changed: `scripts/benchmark-version.sh`
- **Learnings for future iterations:**
  - `run_match` and `run_balanced_match` now take 4 args: label, wrapper, time_ms, inc_ms
  - `LTC_TIME_MS=60000`, `LTC_INC_MS=1000`, `STC_TIME_MS=10000`, `STC_INC_MS=100` are top-level vars
  - US-005 should call `run_match` with `STC_TIME_MS`/`STC_INC_MS` for the second block
  - The `time_control` CSV column is derived by chess-runner from `--time`/`--inc` args, so passing 60000/1000 automatically writes "LTC"
---

## 2026-04-09 - US-005
- Added STC match block after the LTC block in `scripts/benchmark-version.sh`
- Prints "Additional benchmark block: STC" before the 4 STC match runs
- Calls `run_match` with `STC_TIME_MS`/`STC_INC_MS` for all 4 Stockfish levels
- Balanced-position matches remain in their own section using LTC, writing "LTC" to the time_control CSV column
- Files changed: `scripts/benchmark-version.sh`
- **Learnings for future iterations:**
  - The STC block writes to the same `RESULTS_CSV` as the LTC block; rows are distinguishable by the `time_control` column ("STC" vs "LTC"), derived automatically from `--time`/`--inc` args
  - Balanced-position matches in `BALANCED_RESULTS_CSV` remain LTC and are already distinguishable by being in a separate file
  - US-006 (SPRT in STC) is already wired up from US-004 — the SPRT block already uses `STC_TIME_MS`/`STC_INC_MS`
  - US-007 and US-008 need `version-report` to read `time_control` from CSVs and group/label output
---

## 2026-04-09 - US-007
- Changed `run_version_report` to group match rows by `time_control` using `BTreeMap<String, HashMap<String, OpponentStats>>`
- Read `time_control` from CSV index 9 (or `"unknown"` for old-format rows with fewer columns)
- Rendered a separate `### LTC` / `### STC` sub-section per time control under `## Match Results`
- Each sub-section has its own table, overall summary, and best/worst matchup — LTC and STC rows are never silently merged
- Files changed: `chess-runner/src/main.rs`
- **Learnings for future iterations:**
  - `tc_opponent_map` is a `BTreeMap<String, HashMap<String, OpponentStats>>` keyed by `time_control` then `opponent`
  - `BTreeMap` used for time_control key so output order is deterministic (alphabetical: LTC before STC before unknown)
  - Old CSV rows (<10 fields) fall back to `"unknown"` time_control label for backward compat
  - US-008 needs similar grouping for SPRT rows (currently `SprtRow` has no `time_control` field yet)
---

## 2026-04-09 - US-006
- Added "SPRT block: STC" print header before the SPRT run in `scripts/benchmark-version.sh`
- SPRT block was already using `STC_TIME_MS`/`STC_INC_MS` from US-004; only the missing print was needed
- Files changed: `scripts/benchmark-version.sh`
- **Learnings for future iterations:**
  - The SPRT block condition is `if [[ -n "$PREV_ENGINE" ]]` — changes here only apply when `--prev-engine` is provided
  - `time_control` column value ("STC") is derived automatically by chess-runner from `--time 10000 --inc 100` args
  - US-007 and US-008 still need `version-report` to read `time_control` from match and SPRT CSVs respectively
---

## 2026-04-09 - US-008
- Added `time_control` field to `SprtRow` struct in `chess-runner/src/main.rs`
- Read `time_control` from CSV index 10 (falls back to `"unknown"` for old-format rows with <11 fields)
- Grouped SPRT rows by `time_control` using `BTreeMap<String, Vec<&SprtRow>>` for deterministic ordering
- Rendered separate `### STC` / `### LTC` sub-sections per time control under `## SPRT Results`
- Files changed: `chess-runner/src/main.rs`
- **Learnings for future iterations:**
  - SPRT CSV `time_control` is at index 10; `sprt_result` is at index 9
  - Old SPRT rows (<11 fields) fall back to `"unknown"` for backward compat — same pattern as match rows
  - `BTreeMap` used consistently for TC grouping (alphabetical order: LTC before STC before unknown)
---
